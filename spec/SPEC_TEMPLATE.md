# <FEATURE / 契约名> SPEC

- **状态**：planned | implemented | archived
- **领域（feature-domain）**：<如 pricing / auth / settlement>
- **更新于**：<YYYY-MM-DD>
- **负责人**：<who>

## 1. 背景 / 目标

<为什么需要这条契约；要解决什么问题。一两段说清。>

## 2. 契约（对外行为，必须稳定的那部分）

- **输入 / 接口**：<公共 API、签名、入参>
- **输出 / 行为**：<返回值、状态变化、事件、日志、错误语义>
- **不变量 / 边界**：<哪些行为属于"行为不变"的范围，重构不得改动>
- **已知可疑行为**：<是 bug 还是有意、暂时锁住的现状，逐条列；要不要改单独走 issue>

## 3. 验收标准（CI 和人都能据此判定"做完了"）

- [ ] <可测的标准 1>
- [ ] <可测的标准 2>
- [ ] 类型检查 + 测试全绿；行为不变的改动不得让既有测试变红
- [ ] 文档 / 示例已同步

## 4. 实现锚点（implemented 时填）

- 实现：`<path/to/file.py>::<function/class>`
- 测试：`<path/to/test_xxx.py>`
- 文档 / 示例：`<docs/...>`、`<examples/...>`

## 5. 兼容性影响

- **公共 import / API 路径**：<是否变化；变化如何兼容>
- **弃用**：<是否弃用了什么；迁移路径>

## 6. 变更记录

| 日期 | 改了什么 | 关联 issue / PR |
|---|---|---|
| <YYYY-MM-DD> | <初稿 / 落地 / 调整> | #<N> |
